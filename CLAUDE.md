@AGENTS.md

## Claude 전용

- `skills/` 아래 새 스킬 추가 시 `superpowers:writing-skills` 사용.
- 플러그인 레이아웃에 구조적 변경이 필요하면 plan 모드 선호.
- `mcp.servers.json`이 MCP 단일 진실 소스 — `.claude-plugin/mcp.json`이나 `.codex-plugin/mcp.json`을 직접 수정하지 말 것.
