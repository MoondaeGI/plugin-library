# Auto-generated from mcp.servers.json + 스킬 env 선언 — 직접 수정하지 말 것
# .env로 복사해 값을 채운 뒤 `npm run env:apply` 실행.

# 스킬/스크립트 env (process.env로 직접 읽음)
# image-gen: OpenAI Images API 호출용 (design-brand-kit·design-page-image 공유)
OPENAI_API_KEY=
